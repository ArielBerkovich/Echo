Echo is deployed.

Client service: {{ include "echo.clientServiceName" . }}:{{ .Values.client.service.port }}
Server service: {{ include "echo.serverServiceName" . }}:{{ .Values.server.service.port }}

{{- if not .Values.client.ingress.enabled }}
{{- if not .Values.client.route.enabled }}
Enable `client.ingress.enabled`, enable `client.route.enabled` on OpenShift, or port-forward the client service for external access.
{{- end }}
{{- end }}

{{- if .Values.client.route.enabled }}
OpenShift route: https://{{ .Values.client.route.host }}
{{- end }}
